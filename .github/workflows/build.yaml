name: Java Build
on:
  push:
    branches:
      - master
      - '[1-9]+.[0-9]+.x'
  pull_request:
    branches:
      - master
      - '[1-9]+.[0-9]+.x'
jobs:
  build:
    if: github.repository != 'micronaut-projects/micronaut-project-template'
    runs-on: ubuntu-latest
    steps:
       # https://github.com/actions/virtual-environments/issues/709
      - name: "🗑 Free disk space"
        run: |
         sudo rm -rf "/usr/local/share/boost"
         sudo apt-get clean
         df -h

      - name: "📥 Checkout repository"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'          

      - name: "🛠 Build with Gradle"
        id: gradle
        run: |
          ./gradlew build publishAllPublicationsToBuildRepository
          find build
          # VERSION=$(./gradlew properties | grep 'version:' | awk '{print $2}')
          # GROUP_PATH=$(./gradlew properties| grep "projectGroup" | awk '{print $2}' | sed 's/\./\//g')
          # # Find the artifact JAR and POM files in the local repository.
          # ARTIFACTS=$(find build/repo/$GROUP_PATH/*/$VERSION/* \
          #     -type f \( \( -iname "*.jar" -not -iname "*-javadoc.jar" -not -iname "*-sources.jar" \) -or -iname "*.pom" \))
          # echo $ARTIFACTS | tr ' ' ',' | sed 's/,$/\n/'